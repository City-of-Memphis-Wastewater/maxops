# As of this writing, the do_batch command will write from any filetype, reading line by line into MaxOps.
# There is no reason to believe that these commands will function properly, especially after the first error.
# But, it could keep running and handle the error, and it could route to the operating system.
print Is this a security threat?
# ^ This is a proper MaxOps cmd2 shell command as of this writing.
print One Two.
print One Two.
print One #Two.
print Just 'One'? 
print If so, pound/sharp is the  comment character.
print And, the plus sign is the newline character.
print Is this working +now?

This all seems wrong. Check the next most recent batch script.